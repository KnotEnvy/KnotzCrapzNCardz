{
  "project": "Knotz Craps",
  "path": "TableGames/craps",
  "oneLine": "A full craps table for one or two players sharing one screen, with rigid-body dice physics, true casino odds, a strategy workshop for building systems and playing against them, and a felt that looks like cloth under a lamp rather than a diagram.",
  "handoffDate": "2026-08-24",
  "status": "Feature complete and play-tested. Everything is green: 193 unit tests (112 engine, 4 dice, 77 strategy), typecheck, lint with zero warnings, and a production build. This session was a visuals and layout pass, driven through the live app the whole way: dice screenshotted in flight and at rest, every effect layer sampled from the DOM across 40 rolls, and the felt read back at three viewport widths. The long-standing duplicate React key warning is fixed, and a second, more serious id collision was found and fixed while verifying it. The console is clean on a fresh load, including with the strategy workshop open.",
  "whatChangedThisSession": {
    "summary": "Visuals. The table got a real surface, the chips got mass, the box numbers got divided the way a dealer divides them, the proposition column got rebuilt against a live reference, and the two events that end a hand got a payoff worth watching. The reference was a screenshot of Crapsee, a commercial craps app the owner used to play on; the brief was to match its layout structure and typography while keeping our own lit green felt rather than its flat black. That choice was put to the owner explicitly, because repainting the surface would have thrown away the lighting work.",
    "surface": [
      "New src/components/table/Surface.tsx - the furniture: wood with long-grain turbulence, a padded leather bumper that casts a blurred shadow inward onto the cloth, the felt's radial fill, a fine nap and a coarser mottle, the lamp cone, and the corner falloff. Read-only decoration in the same spirit as Fx.tsx: it knows nothing about bets, seats or the store.",
      "TableBed draws under the printed layout and TableLight draws over it. That split is the whole point - light falls on ink, so the corner falloff dims the lettering along with the cloth it sits in. A layout that stays uniformly bright while the felt falls away reads as a sticker.",
      "The lamp sways a few pixels over nineteen seconds, and a live point breathes on the puck. Ambient motion tied to state rather than to a clock, so a come-out is genuinely still."
    ],
    "layout": [
      "A box number is now three printed bands - LAY and D/C above, the number and its payout, PLACE and COME below - and each band is its own hitbox with its own bet kind. 47 clickable zones, up from 31. A player no longer arms a mode before betting the side they meant.",
      "Hover tints the two sides of the layout apart: warm for the right side, cool for the don't side.",
      "Amber box numbers and red don't-side lettering (DON'T COME, DON'T PASS BAR, the barred twelve, ANY SEVEN, ANY CRAPS), matching the reference and real felt. New --color-print-amber and --color-print-red tokens.",
      "The proposition column is printed as a box on darker cloth rather than implied by a hairline, and rebuilt on a named PROP_ROWS grid.",
      "Horn High 2 / 3 / 11 / 12 now have felt cells. They were reachable only from inside the hop dialog before, and the engine already supported them in full.",
      "The dice printed on the hardways and props are crimson with white pips (PRINT_DIE_FACE / PRINT_DIE_PIP in Pips.tsx), matching both the reference and the actual dice the table throws.",
      "Field numbers are centred on their band and spread wider; they used to sit 44px right of centre."
    ],
    "chips": [
      "A stack is no longer the top chip drawn five times. Every chip below the top renders as a side wall - its own colour in shadow, with contrasting edge spots moulded through - and each is turned by a deterministic hash so the spots never line up into a stripe.",
      "Felt bets went 17 to 20, odds 12 to 15, chips in flight 13 to 15, with a tighter two-part contact shadow.",
      "seatOffset is now purely horizontal: the first seat's money to the left, the second seat's to the right."
    ],
    "dice": [
      "Contact-shadow blobs under each die that tighten and darken as it falls, driven straight off the die's height. The mapped shadow alone barely changes weight between resting and airborne, which is the opposite of what sells contact.",
      "A bounce light off the cloth, so the crimson picks up green along its lower edges.",
      "A conservative material polish. See keyDecisions - clearcoat has a ceiling here for a physical reason."
    ],
    "fx": [
      "New OutcomeBurst: a hot core, two offset shock rings and sixteen sparks, fired at the box that decided the hand - the point's own box when it is made, the middle of the layout on a seven out. Drops out entirely under reduced motion.",
      "Chip flights arc. A payoff is tossed with hang time and a little rotation; a loss slides flat and briskly. Same two endpoints, opposite gesture, achieved with separate eases on x and y.",
      "Every effect layer's root carries an inert data-fx hook so a layer can be asserted from outside React."
    ],
    "bugsFoundAndFixed": [
      "The duplicate React key warning that had been open for two sessions. It was not in the felt at all: StrategyWorkshop and NewSessionDialog are siblings in one children array and both remount counters start at 0, so the two collided on key `0` at first mount. Found by walking the live fiber tree for parents with duplicate sibling keys, after captureOwnerStack pointed only as far as Page.",
      "A custom strategy could shadow a house one. saveStrategy appended any strategy whose id was not already in customStrategies, without checking whether that id belonged to a house system, so saving an edit to a house system persisted a custom under the house id. The duplicate key was the mild symptom; the serious one is that allStrategies puts house first, so findStrategy returned the house version and the player's saved edits silently never ran. Fixed at both ends and covered by three new tests.",
      "Sparks were rotated with a transform attribute next to an animated x. motion drives SVG transforms through style, and a CSS transform beats the presentation attribute, so the rotation was being thrown away silently. The angle moved to a plain wrapper element.",
      "Both dice contact blobs shared one material instance, so writing opacity per die would have left both wearing whichever was updated last.",
      "Collecting the two blob refs into an array to map over read refs during render; eslint's react-hooks/refs caught it. They are written out explicitly now.",
      "The horn-high cells and the single-number props both printed bare numbers, so their tooltips and aria-labels were identical and ambiguous. FeltArea gained an optional callName - what a dealer would call the bet - separate from the glyph printed on the felt.",
      "anchorFor duplicated the hop cell's coordinates as literals instead of reading HOP_AREA.anchor, so moving the cell would have left hop chips behind. It reads the anchor now."
    ]
  },
  "origin": {
    "spec": "../../crapsPlan.md",
    "specCoverage": "All items in the plan are implemented. See `deviationsFromSpec` for the three places the implementation differs, and `beyondSpec` for what was added.",
    "productDecisions": [
      "One or two seats hot-seat at one table rather than networked play. The engine was kept transport-agnostic so a websocket layer can be added without a rewrite.",
      "Dice are genuine 3D rigid-body physics, not a 2D animation.",
      "A strategy is data in the same language the player builds in, not code. There is no privileged path for the built-in systems, which is what makes 'duplicate one and pull it apart' the way in to the builder."
    ]
  },
  "stack": {
    "framework": "Next.js 16.3.2 (App Router, Turbopack default), React 19.2",
    "language": "TypeScript 5, strict",
    "state": "zustand 5 + persist middleware; immer 11 used inside the engine rather than as zustand middleware",
    "styling": "Tailwind CSS 4 (CSS-first @theme in src/app/globals.css)",
    "motion": "motion 13 (the Framer Motion successor package)",
    "3d": "three 0.185, @react-three/fiber 9, @react-three/drei 10",
    "physics": "@dimforge/rapier3d-compat (WASM inlined; no loader config needed)",
    "test": "vitest 4",
    "fonts": "next/font/google — Oswald (felt lettering) + Inter (UI)"
  },
  "commands": {
    "dev": "npm run dev",
    "build": "npm run build",
    "test": "npm test — 190 tests, ~15s. Run this constantly.",
    "testStats": "npm run test:stats — seeded house-edge simulations, several minutes. Opt-in, separate vitest config.",
    "typecheck": "npm run typecheck",
    "lint": "npm run lint — currently zero errors and zero warnings; keep it that way, it caught two real bugs before and two more this session.",
    "note": "All commands run from TableGames/craps, not the repository root. The root has no build of its own."
  },
  "architecture": {
    "principle": "The engine is pure. `src/lib/engine` and `src/lib/strategy` contain no React, no I/O, no clock and no randomness at the point of use. `applyRoll(table, roll)` returns a new table, the settlements to animate, and a ledger record; `runStrategy(...)` returns a new table, new memory, and a log. That is what makes the game replayable from a seed and testable without a browser. Keep it that way.",
    "layers": [
      {
        "path": "src/lib/engine/types.ts",
        "role": "Bets, table state, house rules, session stats. Plain data only."
      },
      {
        "path": "src/lib/engine/odds.ts",
        "role": "Every payout ratio in one place, plus the increment rules and chipToWager. The felt and the resolver both read from here, so displayed odds and paid money cannot drift apart."
      },
      {
        "path": "src/lib/engine/rng.ts",
        "role": "xoshiro128** seeded PRNG with rejection sampling for the dice, so no face carries modulo bias."
      },
      {
        "path": "src/lib/engine/resolve.ts",
        "role": "applyRoll — the only function in the codebase that moves money on a roll."
      },
      {
        "path": "src/lib/engine/table.ts",
        "role": "Table construction plus the legal moves between rolls. Each returns either a new table or a refusal with a reason the UI shows as a toast. setBetAmount is the newest and the only one that can reduce a bet."
      },
      {
        "path": "src/lib/engine/stats.ts",
        "role": "Derived figures for the HUD: distribution vs theory, equity curve, per-seat yield."
      },
      {
        "path": "src/lib/strategy/types.ts",
        "role": "The strategy language: triggers, conditions, actions, amounts, memory. A strategy is plain JSON — it persists, exports, and imports."
      },
      {
        "path": "src/lib/strategy/run.ts",
        "role": "runStrategy. Evaluates rules in order and moves money only by calling the same legal-move functions the felt calls, which is what guarantees a bot cannot make a bet a player could not."
      },
      {
        "path": "src/lib/strategy/library.ts",
        "role": "The seventeen house systems, written in the player-facing language, plus strategyWarnings and the duplicate/blank constructors."
      },
      {
        "path": "src/lib/strategy/describe.ts",
        "role": "One describer. The collapsed rule rows, the library cards and the action log all read from it, so the builder cannot claim one thing while the log reports another."
      },
      {
        "path": "src/lib/dice/simulate.ts",
        "role": "Deterministic dice physics. Produces a recorded animation that lands on a predetermined outcome."
      },
      {
        "path": "src/lib/store/useGame.ts",
        "role": "The only thing the UI talks to. Delegates every money move to the engine; it cannot invent a payout. Also owns strategy assignment, per-seat memory, and the autoplay driver."
      },
      {
        "path": "src/components/table/layout.ts",
        "role": "Single source of truth for felt geometry - where each betting area sits, where its chips land (anchorFor), which box lights up when it resolves (rectFor), the three bands inside a box number (numberZones), and the proposition column's row grid (PROP_ROWS)."
      },
      {
        "path": "src/components/table/Surface.tsx",
        "role": "The table's furniture: wood, bumper, cloth and the light over it. Read-only, like Fx. TableBed goes under the printed layout and TableLight over it, which is what keeps the print lit and shaded with the cloth."
      },
      {
        "path": "src/components/table/Fx.tsx",
        "role": "Read-only decoration drawn inside the felt's SVG. Handed a settlement list and a roll record; touches neither the store nor the engine."
      },
      {
        "path": "src/components/strategy/Workshop.tsx",
        "role": "The workshop dialog: library, seat assignment, warnings, export/import."
      },
      {
        "path": "src/components/strategy/RuleEditor.tsx",
        "role": "The when/if/then rule builder, and the shared little editors for targets, amounts and comparisons."
      },
      {
        "path": "src/components/Setup.tsx",
        "role": "One setup form, two frames: the start screen wraps it before anyone sits down, the new-session dialog wraps it afterwards. Now also asks what each seat is playing."
      }
    ]
  },
  "keyDecisions": [
    {
      "topic": "The table is painted in the order light arrives",
      "where": "src/components/table/Surface.tsx, and where TableBed / TableLight sit in Felt.tsx",
      "decision": "Wood, then the padded bumper and the shadow it throws inward, then the cloth and its nap, then the printed layout, then the lamp cone, then the corner falloff. TableLight is rendered after the print, not before it.",
      "why": "Screen-printed ink is lit and shaded along with the cloth it sits in. Put the falloff under the print and the lettering stays uniformly bright while the felt darkens around it, which reads instantly as a sticker laid on top of a table rather than a layout printed into one. Everything else in the file is conventional gradient work; this ordering is the part that actually makes it a surface.",
      "doNot": "Do not move TableLight above the printed layout to 'stop the print being dimmed'. The dimming is the effect."
    },
    {
      "topic": "A stack is chips, not one chip repeated",
      "where": "ChipWall and the spin() hash in src/components/table/Chip.tsx",
      "decision": "Only the top chip of a stack draws an inlaid face. Every chip under it draws a side wall - its own colour in shadow, with contrasting edge spots moulded through - and takes a deterministic rotation derived from its index and the stack's value.",
      "why": "Looking down at a real stack you see one face and a crescent of each chip's edge below it. Drawing the crescent is most of the difference between a stack of chips and a stack of circles. The rotation is hashed rather than random because this component re-renders whenever anything else on the table changes, and Math.random would make every stack on the felt shimmer on an unrelated state change.",
      "doNot": "Do not reach for Math.random here, or in Fx. Hash the index."
    },
    {
      "topic": "A box number is three bands, and each band is its own bet",
      "where": "numberZones in layout.ts, and the zones memo in Felt.tsx",
      "decision": "The cell is split into LAY, the number, and PLACE. Each is a separate hitbox: the top band lays, the bottom band places, and the middle band takes whichever mode the toolbar has armed. Hover tints the don't side cool and the right side warm.",
      "why": "It is how a real layout is divided and how a dealer reads it, and it removes the need to arm a mode before betting the side you meant. The captions teach an empty box; chips cover them once money is down, exactly as they do on a table.",
      "note": "CELL_BANDS must keep summing to ROWS.numbers.h (168). The band heights were rebalanced from 46/76/46 to 48/72/48 purely to give the larger chip room to sit inside a band without touching the printed line."
    },
    {
      "topic": "Seat money goes left and right, and the spread is tuned to the chip radius",
      "where": "SEAT_SPREAD and seatOffset in layout.ts",
      "decision": "The first seat's chips sit 19 units left of the shared anchor, the second seat's 19 right.",
      "why": "The old offset was diagonal, which read as one stack sitting on another and left the rim colour as the only way to tell them apart. Left and right is a distinction you can make at a glance without knowing the colour code. 19 is not arbitrary: the felt draws bets at r=20, so 19 puts a pair just touching. At 15 they overlapped by a quarter.",
      "doNot": "Do not change the chip radius in Felt.tsx without re-tuning SEAT_SPREAD, and vice versa. Nothing type-checks the relationship and the failure is silent overlap.",
      "note": "On the narrowest cells - Small / Tall / All at 64 wide - a pair does overhang the printed line. That was accepted; it happens on a real layout too."
    },
    {
      "topic": "motion writes style.transform, which beats the transform attribute",
      "where": "the spark wrapper in OutcomeBurst, src/components/table/Fx.tsx",
      "decision": "Anything static that positions an animated SVG element - a rotation, a translation - goes on a plain parent element, never alongside motion's own animated transform on the same node.",
      "why": "motion drives SVG transforms through the style property, and a CSS transform overrides the transform presentation attribute entirely. Writing rotate() next to an animated x does not merge them; it silently discards the rotate. This cost a debugging pass and it fails without any error - the sparks simply all fly the same way."
    },
    {
      "topic": "Clearcoat without an environment map darkens rather than polishes",
      "where": "useDieMaterials in src/components/dice/DiceStage.tsx",
      "decision": "clearcoat is held at 0.8 with clearcoatRoughness 0.12, not 1.0 and 0.08.",
      "why": "There is no environment map in this scene. A full-strength coat has nothing to reflect and only costs the diffuse its brightness, so the dice go dark instead of glossy. The higher values were tried, screenshotted, and backed out.",
      "doNot": "Do not raise clearcoat toward 1 on the strength of it being 'more physical'. If you want a real polish, add an environment first, and check it on screen."
    },
    {
      "topic": "Effect layers carry a data-fx hook",
      "where": "the roots of AreaFlashes, ChipFlights, OutcomeBurst and OutcomeWash",
      "decision": "Each layer's root element has an inert data-fx attribute. Nothing in the app reads it; it exists so a layer can be asserted from outside the React tree.",
      "why": "A blank effect layer is exactly the class of bug a green build hides, and catching one in a screenshot means taking it on the right millisecond. With the hooks you can poll the DOM across a run of rolls and count what actually mounted.",
      "note": "Presence is not visibility. These layers deliberately end at zero opacity and stay mounted until the next hand-ending roll replaces them, so a sampler that counts presence will report them as permanently on. Check the opacity too."
    },
    {
      "topic": "A custom strategy can never shadow a house one",
      "where": "allStrategies and saveStrategy in src/lib/store/useGame.ts",
      "decision": "allStrategies filters out any custom whose id is already a house id, and saveStrategy mints a fresh mine-N id rather than writing a custom under a house id.",
      "why": "The library concatenates house first, so findStrategy returns the house version. A custom carrying a house id is therefore unreachable: the player's saved edits silently never run, and every list rendering the library gets duplicate React keys. The filter also repairs sessions that already have such a strategy persisted, which is what saving over a house system used to produce - the owner's session had exactly that.",
      "note": "Regression tests: the 'the library a player actually sees' block."
    },
    {
      "topic": "A strategy is a list of when / if / then sentences",
      "where": "src/lib/strategy/types.ts",
      "decision": "A rule is a trigger, a list of ANDed conditions, and an ordered list of actions. Rules run top to bottom at every chance to bet. There is no boolean tree and no scripting language.",
      "why": "Every real craps system a player describes out loud is a handful of those sentences. An 'or' is two rules, which reads better in the workshop than nesting and costs nothing to run. Keeping it declarative is also what lets the describer render a rule back into English and the validator warn about rules that cannot work.",
      "doNot": "Do not add an expression language or user-supplied JavaScript. The whole safety argument — that a bot can only make bets a player could make by hand — rests on actions being a closed set that goes through table.ts."
    },
    {
      "topic": "A bet names a level, not a helping",
      "where": "the BET action in run.ts, and `topUp`",
      "decision": "BET brings a spot *up to* the amount named. `topUp: true` is the opt-in for adding on top. UNITS, FIXED, TABLE_MIN and PCT_BANKROLL are 'level' modes and get the difference taken off what is already there; the press-style modes are already deltas and pass through.",
      "why": "Place bets survive a point being made, so a rule saying 'twenty-two inside' fires again on the next point of the same hand. Adding made it $44, then $66. Bringing it up to a level is also what a player means when they say it. Found by probing two point cycles; a single cycle looked perfect.",
      "doNot": "Do not switch the default back. Regression tests: the 'betting a spot that already has money on it' block."
    },
    {
      "topic": "`once` counts successes, not attempts",
      "where": "the epoch stamp at the end of the rule loop in run.ts",
      "decision": "A once-per-point or once-per-hand rule is only marked as fired if one of its actions actually did something.",
      "why": "Two reasons. A rule refused for want of chips has not had its turn and should try again next roll. And it is what lets a regression rule sit *above* the rule that creates the bets it regresses: on the roll that sets the point the regression finds nothing to do, so its budget survives for the real hit. That ordering is load-bearing in $44 Inside Regression and there is a comment saying so.",
      "doNot": "Do not stamp on attempt. It will not fail a typecheck; it will make the regression fire on the point-set roll and flatten the strategy."
    },
    {
      "topic": "State triggers versus event triggers",
      "where": "triggerFires in run.ts, STATE_TRIGGERS in types.ts",
      "decision": "COME_OUT, POINT_ON, HAND_START and EVERY_ROLL describe how the table is sitting and may fire again on an unchanged table. POINT_SET, POINT_MADE, SEVEN_OUT, NUMBER_HIT, CRAPS and NATURAL describe what the last roll did and fire once, on that roll only. A manual Run passes `force`, which re-enables the state triggers but never the event ones.",
      "why": "Pressing Run twice after an eight must not press the eight twice. But a player who took a bet down and pressed Run should get it back. The two classes of trigger are exactly that distinction.",
      "note": "All seventeen house systems key their opening bets off POINT_ON rather than POINT_SET, paired with `once: POINT`. Both fire on the roll that establishes a point, but only the state trigger also fires when a system is dropped onto a seat halfway through a hand — otherwise the seat sits out the rest of the hand doing nothing. POINT_SET is still in the builder for rules that genuinely mean 'the moment it happened'."
    },
    {
      "topic": "The strategy layer lives outside TableState",
      "where": "seatStrategy / strategyMemory / customStrategies on the store, not on the table",
      "decision": "The engine has no idea a bot is playing. Assignment and memory are store state; the table is untouched.",
      "why": "It keeps the engine pure and — usefully — meant `version` in the persist config stayed at 3. The table's shape did not change, so a saved session from before the feature is still perfectly good; it simply arrives without the new keys and `merge` fills them from the defaults. There is an added guard that takes a saved seatStrategy or strategyMemory only if both seats are present.",
      "doNot": "Do not move strategy state onto TableState for convenience. It would force a version bump and throw away every in-progress session for nothing."
    },
    {
      "topic": "Autoplay runs synchronously when a roll settles",
      "where": "playStrategies, called from settleDice and the fast-roll path in useGame.ts",
      "decision": "No timer, no delay. Both roll paths apply the roll, record what came down, then give each auto seat its turn, then commit once.",
      "why": "A delay was tried in the design and rejected: the bets have to be on the felt before the player can throw again, and a timer either fights the settlement animation or loses the race against someone hammering the roll button in fast mode. Fast mode exists precisely to grind a strategy over hundreds of rolls, so the synchronous path is the important one. The FX are read-only decoration and cope fine with new chips appearing mid-flash.",
      "note": "`recordAction` is deliberately handed the table the roll produced, *before* any strategy acts, so Same Action repeats what came down rather than what a bot just put up."
    },
    {
      "topic": "A bot moves money only through the legal moves",
      "where": "every action in run.ts goes through placeBet / setOdds / setBetAmount / takeDown / setWorking",
      "decision": "There is no path in the strategy runner that touches a bankroll directly.",
      "why": "It means a strategy inherits the table minimum, the increments, the rack check, the contract-bet rules and the odds caps for free, and cannot invent a bet a player could not make. It also gives a sharp invariant to test: everything a strategy does between rolls moves chips between the rack and the felt, so a seat's total worth must come out exactly where the settlements left it. Every one of the seventeen systems is checked against that over a seeded 400-roll session.",
      "doNot": "Do not set `opts.fromChip` in a strategy action. Strategy amounts are already stated in the spot's own units; converting them again silently inflates the bet. This is the same trap the felt has."
    },
    {
      "topic": "Unit arithmetic gives the classic dollar names for free",
      "where": "the UNITS amount mode, resolved in run.ts and snapped by placeBet",
      "decision": "A strategy carries a base unit and bets in multiples of it. No dollar figure like '$22 inside' is written down anywhere.",
      "why": "The engine's own increment rules do the work: one $5 unit on the six goes up as six dollars, so one unit inside is 5+6+6+5 = $22 and two units is 10+12+12+10 = $44. One unit across is $32. The same strategy therefore plays a $5 table and a $25 one. Tests assert those three figures exactly, which is a nice canary on the increment logic as a whole."
    },
    {
      "topic": "House systems are code; the player's are data",
      "where": "HOUSE_STRATEGIES in library.ts, customStrategies in the store",
      "decision": "The library the UI shows is the built-ins concatenated with the persisted custom ones. Only the custom ones are stored.",
      "why": "A fix to a house system reaches a returning player instead of being shadowed by the copy their browser saved months ago — which matters, because this session fixed a trigger in six of them.",
      "note": "The flip side, and a real gotcha: a duplicate snapshots the house rules at the moment it was copied. Fixing a house strategy does not reach existing duplicates of it."
    },
    {
      "topic": "One describer, used everywhere",
      "where": "src/lib/strategy/describe.ts",
      "decision": "describeRule generates the sentence for the collapsed rule row, the read-only house listing and the action log. Conditions use a separate noun form (targetNoun) from actions (targetLabel).",
      "why": "If the builder and the log rendered a rule independently they would eventually disagree, and the log is the only window onto what a bot is doing. The two noun forms exist because 'bet on the pass line' and 'my pass line bet is not up' want different shapes; stacking one on the other produced 'I have no the pass line'.",
      "note": "One test asserts a full generated sentence verbatim. If you change the phrasing, that test is the thing that will tell you."
    },
    {
      "topic": "Dice that are both physically real and RNG-determined",
      "where": "src/lib/dice/simulate.ts",
      "decision": "The RNG picks the result before the throw. One physics simulation runs with arbitrary initial conditions; whichever face lands up, a cube-symmetry rotation R is applied to every recorded orientation so the wanted face occupies that slot.",
      "why": "A uniform cube has an isotropic inertia tensor and a cube collider is invariant under the 24 rotations of the octahedral group, so q0 -> q0*R yields a provably identical trajectory with q(t) -> q(t)*R. Real tumble, exact outcome, no rejection sampling and no snap at the end.",
      "doNot": "Do not switch to live physics in the render loop and read the resting face. It reintroduces bias risk, cocked dice and non-replayable sessions."
    },
    {
      "topic": "A chip is worth what the spot bets in",
      "where": "chipToWager in odds.ts, called from placeBet only when opts.fromChip is set",
      "decision": "Chip denominations convert to the spot's own units: the six and eight take five-dollar units as six-dollar ones, so $25 becomes $30 and $100 becomes $120. Everything else snaps up to its increment.",
      "why": "This is how the bet is actually made — nobody says 'twenty-five on the six' and means twenty-five. Rounding up rather than down matches a dealer taking the extra dollar rather than handing change back.",
      "doNot": "Do not set fromChip on presses, power presses, Same Action or anything in the strategy runner. Those amounts are already stated in the spot's units, and converting them again inflates the bet — a $30 six pressed by $6 would become $42 instead of $36. Regression tests: the 'what a chip is worth on the felt' block."
    },
    {
      "topic": "Pressing is a call about one number",
      "where": "pressNumber / powerPressNumber in table.ts, driven from lastRoll in useGame",
      "decision": "Press and Power Press take the number the last roll landed on, and refuse with a reason if it was not a box number or the seat has nothing there.",
      "why": "The previous pressAll/powerPress reached across the whole layout and spent the rack on numbers that had done nothing. That is not what 'press it' means at a table.",
      "note": "The strategy language expresses the same idea with the HIT number reference, which is why 'press the number that hit by one unit' is a single rule rather than six."
    },
    {
      "topic": "Under the minimum is taken at the minimum",
      "where": "placeBet in table.ts",
      "decision": "A wager below rules.minBet is raised to the minimum rather than refused. Props and hops stay exempt — a dollar on the yo is a real bet.",
      "why": "Refusing was technically correct and practically annoying. The order of adjustments is deliberate: chip conversion, then the minimum, then the increment, then the rack check.",
      "note": "There is a deliberate step-down: if rounding up would overdraw the rack, placeBet falls back to the largest payable total the seat can cover ($28 rack, quarter on the six, takes $24). Do not remove it — without it a short rack simply cannot bet."
    },
    {
      "topic": "Roll history must stay outside immer's produce",
      "where": "src/lib/engine/resolve.ts, end of applyRoll",
      "decision": "The history window is rebuilt with plain array operations after `produce` returns, then Object.freeze'd before being handed back.",
      "why": "Measured: mutating a 600-entry history inside the draft cost 118 rolls/sec versus 2462 after the change, a 21x cliff. Immer proxies every element of an array it sees mutated. Freezing it ourselves also lets immer's deep-freeze short-circuit on the next produce.",
      "doNot": "Do not move `history.push` back inside the draft. It will not fail any test; it will just make the game crawl after a long session."
    },
    {
      "topic": "Bet ids live on the table, not in a module counter",
      "where": "TableState.betSeq, used by placeBet",
      "decision": "`betSeq` is part of the persisted table state.",
      "why": "A module-level counter resets on page load while restored bets keep their old ids, so new bets collided with them. That produced duplicate React keys and, worse, take-down and odds calls that addressed the wrong bet. Regression test: 'keeps issuing unique bet ids after a session is saved and reloaded'.",
      "note": "Custom strategy ids follow the same reasoning: `customSeq` is on the store, and rule ids are derived from the strategy id plus position so they are stable across reloads. A resetting counter would re-arm every once-per-session rule a restored table had already spent."
    },
    {
      "topic": "Settlements carry their own location",
      "where": "Settlement.at (BetLocation) in types.ts",
      "decision": "Each settlement records where the bet was sitting.",
      "why": "By the time the UI animates a loss, the bet has been swept off the table. This is also exactly what makes the chip-flight layer possible, and what lets a strategy size a press by what the number just won."
    },
    {
      "topic": "Effects are keyed on the roll, and the first roll of a session is not replayed",
      "where": "Fx.tsx, and `openingRoll` in Felt.tsx",
      "decision": "Each effect layer mounts fresh per roll and ends at zero opacity, rather than being wrapped in AnimatePresence. Felt records the roll index present at mount and treats only later rolls as live.",
      "why": "A restored session arrives with its last roll already in table.history. Without the opening mark, reopening the tab replayed the previous sitting's finish — a gold wash, or the whole table shaking for a seven out from yesterday."
    },
    {
      "topic": "Dialogs are remounted rather than synced with an effect",
      "where": "`key` on NewSessionDialog and StrategyWorkshop in page.tsx",
      "decision": "Opening a dialog bumps a counter used as its React key, so plain initial state already lands on the right thing.",
      "why": "It is the established pattern here and it keeps setState out of effects, which the lint rule react-hooks/set-state-in-effect enforces. The workshop originally used two syncing effects and both were lint errors."
    },
    {
      "topic": "The seven-out shake uses animation controls, not a remount",
      "where": "Felt.tsx, useAnimationControls fired from an effect",
      "decision": "The felt is wrapped in a motion.div whose transform is driven by controls.",
      "why": "Keying the felt to force a replay would remount every chip, restarting each entrance spring and re-running the whole layout — a lot of work to move the table six pixels."
    },
    {
      "topic": "Settlement figures clear on a timer",
      "where": "holdSettlements in useGame.ts",
      "decision": "The settlement list is emptied 2.6s after a roll, or 1.8s after a seven out.",
      "why": "They used to sit until the next roll, which on a slow table meant stale numbers over live bets.",
      "doNot": "Do not shorten these below ~1.5s without re-checking Fx.tsx — the win flash alone runs 1.2s."
    },
    {
      "topic": "The ON/OFF puck is one flag, reset at each phase change",
      "where": "Bet.working plus applyPhaseDefaults in resolve.ts",
      "decision": "`working` is the actual live state. The engine resets it to the house default whenever the phase changes.",
      "why": "This matches a real table: you have to tell the dealer again each come-out."
    },
    {
      "topic": "Chart colours are validated, not eyeballed",
      "where": "src/components/hud/Charts.tsx",
      "decision": "Seat series are #c08a14 and #3298dd — one step deeper than the chips on the felt.",
      "why": "Ran through a CVD validator against the dark panel surface: protan deltaE 24.6, tritan 22.3, contrast pass. If you change these, re-validate rather than eyeball."
    },
    {
      "topic": "The dice canvas renders continuously, on purpose",
      "where": "src/components/dice/DiceStage.tsx, the Canvas frameloop prop",
      "decision": "frameloop is the explicit string 'always'.",
      "why": "R3F's on-demand mode was tried. It built clean, type-checked clean and linted clean — and left the canvas blank after a throw settled, so the resting dice never appeared. Reverted to the mode that was visually verified.",
      "doNot": "Do not re-attempt this on the strength of a green build. It is a rendering change; it needs a screenshot of the settled dice to be considered working."
    }
  ],
  "gotchas": [
    "fastRoll is persisted, and the fast path in throwDice deliberately sets animation to null. If the dice never appear, check the FAST button before you go looking at the canvas - a saved session with fast mode left on looks exactly like a broken renderer. This cost real debugging time this session.",
    "SEAT_SPREAD in layout.ts is tuned against the chip radius Felt.tsx passes to ChipStack (20). Change one without the other and the two seats' stacks silently overlap again.",
    "CELL_BANDS must sum to ROWS.numbers.h. PROP_ROWS is a chain of hand-checked offsets and the proposition column is packed to within a few pixels; add a row and something has to give height back.",
    "PrintedLayout is React.memo'd and carries the printInk filter. It takes no props on purpose. Put per-roll state inside that group and you will re-rasterise a filter over the whole table on every roll.",
    "The .felt-text class sets text-anchor in CSS, and a CSS declaration beats the text-anchor presentation attribute. A caption that needs to hang off an edge has to use .felt-text--start / --end, not textAnchor.",
    "FeltArea.label is the glyph printed on the felt; FeltArea.callName is what the bet is called. On the props those differ - the horn high two and the two both print '2' - and the zone name, tooltip and aria-label all read callName first. Add a prop cell without a callName and it will be indistinguishable to a screen reader.",
    "When verifying in the browser pane, the console buffer survives a reload. Stale duplicate-key errors will sit there long after the fix. Use a fresh tab, or walk the fiber tree for parents with duplicate sibling keys - that is ground truth, and it is how both of this session's key bugs were located.",
    "src/lib/dice/simulate.ts FACE_AXES and src/components/dice/DiceStage.tsx MATERIAL_ORDER encode the same die convention (+X=1, -X=6, +Y=2, -Y=5, +Z=3, -Z=4). If they disagree, every roll renders a face that does not match the result the engine settled. Nothing type-checks this; the dice test suite does.",
    "Changing the shape of TableState requires bumping `version` in the persist config in useGame.ts. It is still 3 — the strategy layer was deliberately kept off TableState, so adding it changed the store's shape without changing the table's. There is a `merge` guard that falls back to fresh defaults for anything missing or half-shaped.",
    "placeBet's `opts.fromChip` is the difference between a rack denomination and an amount already in the spot's units. Getting it wrong does not fail typecheck and does not look wrong on screen — it just quietly bets more than the player asked for. Nothing in src/lib/strategy sets it, and nothing should.",
    "A custom strategy is a snapshot. Duplicating a house system copies its rules as they are at that moment; a later fix to the house version does not reach the copy. This session's POINT_SET -> POINT_ON fix is a live example — duplicates made before it still carry POINT_SET and will sit out a hand they were assigned into.",
    "Rules run top to bottom and each one sees what the rules above it just did. That is deliberate and $44 Inside Regression depends on it: the regression rule sits above the rule that creates the bets, so on the roll that sets the point it finds nothing and keeps its once-per-point budget for the real hit. Reordering those two rules breaks the strategy without failing a typecheck.",
    "The strategy log deduplicates consecutive identical refusals, so a broke seat reaching for the same bet every roll says so once. If you are wondering why a refusal only appears once, that is why.",
    "Simulations should pass rules.historyLimit = 0. It skips the history window copy and roughly triples throughput. runStrategy takes the RollRecord as an argument rather than reading table.history precisely so it still works in that mode.",
    "Prop and field bets ride after a win by default (rules.propsRideAfterWin), which is what a real table does. Betting the same spot again tops the existing bet up rather than replacing it — at the engine level. The strategy layer's BET is the one that means 'bring up to a level'.",
    "The engine is loaded with immer's auto-freeze left ON. Do not disable it for speed; the history fix already recovered the throughput and the freeze catches accidental mutation.",
    "Solo is TableState.solo, not a rule. resolve.ts checks it before passing the dice, and SeatBar, the HUD, the equity chart and the strategy layer all narrow to one seat from it. Seat B still exists in state so nothing downstream has to handle a missing seat, and playStrategies skips it explicitly.",
    "next dev rewrites the agent block at the top of AGENTS.md on every run. Committing it with your work keeps the tree clean.",
    "The dice test 'is fast enough to run synchronously on a click' asserts under 60ms per throw and measured 61.5ms once on a machine also running a dev server and a browser. It passes comfortably on its own. If it fails, re-run it alone before believing it.",
    "A green build, typecheck and lint prove nothing about whether the 3D canvas actually drew anything, whether an effect layer animated, or whether a bot actually put chips on the felt. All three were verified this session by driving the live app and reading the DOM back. That is the standard to hold."
  ],
  "verification": {
    "automated": {
      "unit": "112 tests. Every bet's payout, come-out and point cycles, contract bets, come-bet travel and come-out protection, working/off behaviour, buy and lay commission both ways, Fire Bet, All/Tall/Small, shooter rotation, solo play, chip denomination conversion, the table minimum auto-fill, the short-rack step-down, the grouped place calls, number-scoped pressing, and an invariant that a bankroll never moves except by the settlements the engine reports.",
      "strategy": "77 tests. Everything the previous session covered, plus a new 'the library a player actually sees' block: a custom carrying a house id never shadows the house version, every id in the library is unique, and a custom with its own id survives.",
      "dice": "4 tests. All 36 combinations land exactly as requested, both dice settle flat and inside the rails over 40 throws, tumble length is watchable, and a throw solves in under 60ms.",
      "statistical": "npm run test:stats — 6 tests, ~165s. Not re-run this session; the strategy layer cannot change a payout, because it only moves chips between a rack and the felt through calls the felt already made. Worth a run before shipping anyway.",
      "static": "tsc --noEmit clean; eslint clean (0 errors, 0 warnings - it caught a refs-during-render bug this session); next build succeeds (4 static routes)."
    },
    "manual": [
      "Duplicate React key warning: located by walking the live fiber tree, fixed, and confirmed gone - zero duplicate-keyed parents in the tree, and a clean console on a fresh tab with the strategy workshop open, which is where the second collision rendered.",
      "Dice screenshotted mid-tumble and at rest, with the new contact shadows visible under both. Re-checked after the ref restructure, because nothing automated catches a blank canvas.",
      "Dice material: clearcoat 1.0 was tried, screenshotted, judged too dark, and backed out to 0.8.",
      "Effect layers sampled from the DOM via their data-fx hooks across 40 rolls: point-made cores 3, seven-out cores 16, sparks 19, rings 19, washes 23, box flashes 51, and up to 9 chips airborne at once mid-arc.",
      "Seat split verified by reading chip transforms back: on every box number, seat A at x and seat B at x+38, identical y.",
      "Horn high driven end to end: clicked on the felt, accepted by the engine, chip landed in the new cell at the correct seat offset.",
      "Layout measured at 1366x768 and 1024x680: no horizontal or vertical overflow, roll button inside the viewport at both.",
      "Previously: strategy layer driven through the real UI; dice confirmed rendering in flight and at rest; win and loss flashes screenshotted in both polarities; chip and minimum rules driven through the real UI."
    ],
    "notVerified": [
      "Reduced motion. The paths are written - OutcomeBurst returns null, the lamp goes static, the puck pulse is guarded - but they were not exercised with the media query actually on. This is the most likely place for a visual regression to hide.",
      "Motion blur on the dice in flight was deliberately not attempted. Real blur needs post-processing, and a ghost trail on fast-tumbling dice reads as a rendering fault rather than speed.",
      "Screenshots were all taken at the browser pane's own viewport (around 830px wide). Wider layouts were checked by DOM measurement, not by eye, because the pane would not composite an emulated viewport.",
      "Touch input, and any viewport narrower than ~1000px.",
      "Safari and Firefox. Only Chrome was used.",
      "Audio was never listened to; nothing was added to it this session, so every new effect is silent.",
      "The expected value of each house system is still not measured anywhere."
    ]
  },
  "deviationsFromSpec": [
    {
      "item": "shadcn/ui",
      "what": "UI primitives are hand-rolled in src/components/ui/primitives.tsx on shadcn's foundation (class-variance-authority + clsx + tailwind-merge) rather than generated by the shadcn CLI.",
      "why": "The table needs a casino aesthetic, not shadcn's default one. The API and dependencies match, so `npx shadcn add` still works if you want a component."
    },
    {
      "item": "zustand immer middleware",
      "what": "immer is used inside the engine's `produce` calls rather than as zustand middleware.",
      "why": "The engine owns all nested mutation and is pure; adding the middleware on top would be a second, redundant layer."
    },
    {
      "item": "Put bets",
      "what": "A new pass or don't-pass bet is refused once the point is on ('The line is closed once the point is on').",
      "why": "Most casinos allow put bets but they are a bad bet and a confusing UI affordance. Come is available and is the correct play. Change `canBet` in table.ts if you want them. Note that strategyWarnings flags a rule that tries it."
    }
  ],
  "beyondSpec": [
    "The strategy workshop: a rule language, seventeen house systems, a visual builder, per-seat autoplay, win goals and loss limits, validation warnings, and JSON export/import.",
    "Fire Bet (24/249/999 to 1) and All/Tall/Small side bets, with their own felt spots.",
    "All 21 hop bets and the four horn-high calls, grouped by total behind the Hop / Horn panel.",
    "Seeded dice: a named seed replays an identical session, so two betting strategies can be compared against the same dice — which is now something the game can do by itself.",
    "A start screen and setup form covering seats, strategies, buy-in, re-buy, limits, odds multiple and every house rule.",
    "Solo play.",
    "Grouped place calls: inside, outside, across.",
    "House rules panel: limits, odds multiple (1x to 100x), field 2x/3x on the twelve, vig on win vs up front, come-out working defaults, increment snapping, side bets on/off.",
    "Session persistence to localStorage.",
    "Keyboard shortcuts (space to roll, 1-7 chips, A/B seats, O max odds, H stats, S run strategy).",
    "Synthesised audio — dice clacks scaled by impact velocity, chip drops, payout and seven-out calls. No audio assets in the bundle.",
    "House edge shown in the tooltip on every box number.",
    "A painted table surface: wood with grain, a padded bumper casting onto the cloth, felt nap and mottle, an overhead lamp that sways, and corner falloff that dims the print with the cloth.",
    "Table juice: box flashes, chips arcing to the rail and raked to the bank, a seven-out shake, full-felt washes, and a burst of rings and sparks at the box that decided the hand. All reduced-motion aware.",
    "Box numbers divided into three labelled, separately clickable bands, so laying, buying and placing are distinct targets rather than a mode you arm first.",
    "Horn High bets printed on the felt."
  ],
  "openItems": [
    {
      "priority": "high",
      "item": "Audio has fallen a long way behind the visuals.",
      "detail": "src/lib/audio.ts is synthesised and has still never been listened to. Nothing was added to it this session, so the box flashes, the chip flights, the outcome burst, the seven-out shake and every strategy call are all silent. The visuals now carry a lot of weight the sound does not, and that gap is the single most noticeable thing missing from the polish. A rake on a loss, a chip drop on a press, and something with real low end on the burst are the obvious gaps. Levels and the seven-out sting are still guesses."
    },
    {
      "priority": "high",
      "item": "Exercise reduced motion for real.",
      "detail": "Every new effect has a reduced-motion path and none of them were run with the media query actually on. Chrome devtools can force prefers-reduced-motion. Check: OutcomeBurst disappears entirely, the lamp stops swaying and renders as a plain rect, the puck stops breathing, chip flights drop out, and the flashes become cross-fades. This is the likeliest place for a regression to be sitting right now."
    },
    {
      "priority": "medium",
      "item": "Dice motion blur, or a better idea.",
      "detail": "Deliberately skipped. Real blur needs post-processing and a naive ghost trail on tumbling dice reads as a rendering fault. If someone wants to take it on, the honest options are a postprocessing pass (new dependency, and the canvas is already always-on) or shaping the tumble so the fastest part of the throw is shorter. Do not ship a trail without looking at it frame by frame."
    },
    {
      "priority": "medium",
      "item": "The hop bets are still behind a dialog.",
      "detail": "The reference app prints a full hop grid and a hardway-hops block on the felt. We have a HOP cell that opens a dialog instead. The proposition column is now packed, so putting 21 hop cells on the felt means either reclaiming space or widening the column, which pushes into the player layout. Worth doing only if the owner wants it; the dialog is genuinely easier to use."
    },
    {
      "priority": "medium",
      "item": "Measure what the seventeen systems actually cost.",
      "detail": "Unchanged from last session and still the natural next step for the strategy layer. The engine is seeded and pure, runStrategy is pure, and the stats config already runs long simulations. A test that plays each system over a few hundred thousand rolls and reports net per roll against total action would turn the workshop from a toy into a lab, and give it somewhere to show an expected-value figure."
    },
    {
      "priority": "medium",
      "item": "Touch and small-screen support.",
      "detail": "Still unaddressed, and the felt got denser this session rather than looser: a box number is now three stacked hit zones roughly 48 units tall, which at a phone width is well under a comfortable tap target. The stats panel already becomes a drawer below 1280px, which is the pattern to follow. The rule builder remains the densest thing in the app."
    },
    {
      "priority": "low",
      "item": "A felt colour option.",
      "detail": "The owner was offered black-felt and dual-palette options this session and chose to keep the green and match the reference's structure instead. If it ever comes back, the groundwork is done: the surface is one module and the print colours are tokens (--color-print-amber, --color-print-red, --color-felt-line), so a second palette is a token swap plus new gradient stops in Surface.tsx, not a rewrite."
    },
    {
      "priority": "low",
      "item": "Chips overhang the narrowest side-bet cells.",
      "detail": "Small / Tall / All are 64 units wide and a two-seat pair spans about 78. Accepted deliberately - it happens on a real layout - but if it ever looks wrong, the fix is a smaller radius for the 'side' variant rather than a narrower seat spread, which is tuned for the box numbers."
    },
    {
      "priority": "low",
      "item": "Idle GPU cost of the always-on canvas.",
      "detail": "Unchanged. The canvas redraws at 60fps even when nothing moves, and the felt now also runs a slow lamp sway and a puck pulse, though those are cheap CSS-level animations rather than GPU work. On-demand rendering did not work as a one-line prop change (see keyDecisions); the likely shape is frameloop='demand' plus an explicit invalidate() from an effect keyed on the animation, and it must be verified with a screenshot of settled dice."
    },
    {
      "priority": "low",
      "item": "The strategy language has no OR inside a rule.",
      "detail": "Deliberate - an or is two rules, and that reads better in the workshop than a nested tree. If it is ever added, a flat 'any of these' list alongside `all` is the shape that would not wreck the describer."
    },
    {
      "priority": "low",
      "item": "A strategy cannot control come odds working through the come-out.",
      "detail": "Bet.oddsWorking is engine-managed and applyPhaseDefaults sleeps come odds through a come-out, which is correct default behaviour. There is no action to override it. The WORKING action only reaches place, buy and hardway bets."
    },
    {
      "priority": "low",
      "item": "The setup form and House rules overlap.",
      "detail": "Setup.tsx and SettingsDialog offer nearly the same fields from different components. Setup writes a whole new table; House rules edits the live one. Worth folding SettingsDialog onto SetupForm with a read-only flag for the fields that cannot change mid-session."
    },
    {
      "priority": "low",
      "item": "Online play.",
      "detail": "The engine is pure, seeded and replayable, and settlements are self-describing, so the natural shape is one authoritative table applying rolls and broadcasting {roll, settlements} to clients. Nothing in lib/engine needs to change, and the strategy layer is a client concern."
    },
    {
      "priority": "low",
      "item": "A dealer/tutorial mode.",
      "detail": "Closer than ever. The engine returns a plain-English reason for every refused action, describe.ts renders any rule as a sentence, and the felt now labels its own bands and names every bet through callName, so a coaching layer has real copy to draw on."
    },
    {
      "priority": "low",
      "item": "Fire Bet progressive payouts.",
      "detail": "Currently resolves only at seven-out. Some houses pay as each qualifying point is made."
    }
  ],
  "repoNotes": {
    "git": "The repository is rooted at KnotzCrapzNCardz and published at https://github.com/KnotEnvy/KnotzCrapzNCardz (public).",
    "structure": "Games are self-contained apps under their own folders. TableGames/craps carries its own package.json, .gitignore and README; the root carries a repo-wide .gitignore and an index README. There is no root build.",
    "siblingAssets": "../../cardArt holds a full 52-card PNG deck. Unused by craps; it is there for the card games the repo name promises. SlotsGames/ is reserved and empty.",
    "ignored": "stats-run.txt and bench-out.txt are local test artefacts and are gitignored."
  },
  "pickUpHere": {
    "firstCommand": "cd TableGames/craps && npm install && npm run dev",
    "firstCheck": "npm test, then open the app and press space a few times. If the dice do not appear, check the FAST button before anything else - fast mode is persisted and deliberately skips the animation. Everything was green at handoff, so anything red is something you changed.",
    "thenRead": [
      "README.md for how the game plays and how to build a strategy",
      "keyDecisions in this file before changing the engine, the dice, the strategy runner, or how a wager is sized"
    ],
    "safestPlacesToStart": [
      "src/components/table/Surface.tsx - the whole table surface is gradients and filters in one read-only file, and nothing depends on it.",
      "src/components/table/Fx.tsx - read-only decoration, no store or engine access, and each layer now carries a data-fx hook you can assert against.",
      "src/lib/strategy/library.ts - adding a house system is data, and the whole library is checked by an invariant sweep the moment you add it.",
      "src/lib/engine - pure, fully tested, and the tests run in seconds.",
      "src/components/table/layout.ts - felt geometry is data, but read the CELL_BANDS and SEAT_SPREAD notes first; several constants in there are tuned against each other."
    ],
    "riskiestPlacesToChange": [
      "src/components/dice/DiceStage.tsx - nothing automated catches a blank canvas, and the material values are tuned by eye against a scene with no environment map.",
      "src/lib/engine/resolve.ts - the ordering comment at the top of applyRoll is load-bearing; come-bet protection depends on it.",
      "src/lib/strategy/run.ts - the level-versus-delta split in BET, the success-only `once` stamp, and the state-versus-event trigger split are all subtle and only one fails loudly.",
      "placeBet in src/lib/engine/table.ts - four adjustments in a deliberate order, and fromChip is silent when misused.",
      "The persist config in useGame.ts - bump `version` whenever TableState changes shape.",
      "Anything animated with motion inside the SVG - motion writes style.transform, which silently overrides a transform attribute on the same element."
    ]
  }
}